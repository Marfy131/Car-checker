version: '3.8'

services:
  app:
    build: .
    ports:
      - "${APP_PORT:-8180}:8080"
    volumes:
      - db:/opt/carwatch/data
      - logs:/opt/carwatch/logs
    env_file:
      - .env
    environment:
      SPRING_PROFILES_ACTIVE: prod
    restart: unless-stopped

volumes:
  db:
  logs:
